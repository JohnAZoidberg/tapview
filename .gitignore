target
result

# Android build outputs (Gradle + the cargo-ndk-produced .so).
/android/.gradle
/android/build
/android/app/build
/android/app/src/main/jniLibs
/android/local.properties
/android/.kotlin

# Trunk's browser-build output (`trunk build` / `trunk serve`).
/dist
